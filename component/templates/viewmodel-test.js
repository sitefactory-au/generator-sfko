define(['components/<%= name %>/<%= name %>'], function(<%= name %>) {
  var ViewModel = <%= name %>.viewModel;

  describe('<%= name %>  view model', function() {

    it('You should implement some tests', function() {
      expect(false).toBe(true);
    });

  });

});
