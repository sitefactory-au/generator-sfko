define(['models/<%= name %>/<%= name %>'], function(<%= name %>) {
  var Model = <%= name %>.model;

  describe('<%= name %> model', function() {

    it('You should implement some tests', function() {
      expect(false).toBe(true);
    });

  });

});
