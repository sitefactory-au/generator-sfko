define(['modules/<%= name %>/<%= name %>'], function(<%= name %>) {
  var Module = <%= name %>.module;

  describe('<%= name %> module', function() {

    it('You should implement some tests', function() {
      expect(false).toBe(true);
    });

  });

});
